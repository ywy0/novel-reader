# Changelog

## 1.2.1 - 2026-09-04

可靠性优化一轮:

- 批量导入保留嵌套目录结构,并跳过 `.novel-reader` 数据目录
- 同名文件不再静默覆盖,导入失败会明确提示
- 避免把书库导入到自身时产生递归复制
- 补齐 `openFolder` / `importBook` / `importFolder` / `bookmarks` 等命令注册
- 自动书签状态按书籍路径隔离,修复切换小说后进度串书
- 扩展停用时清理计时器并保存有效停留位置
- 数据文件改为按书籍的目录结构存放,避免 `a/b.txt` 与 `a__b.txt` 同名冲突;继续兼容旧版扁平数据文件
- 数据写入采用临时文件替换,降低 JSON 损坏概率
- 主题「默认」恢复用户原本主题,不再强制切换 `Default Dark Modern`
- 修正首章之前被错误高亮为第一章的问题

## 1.2.0

- 主题循环按钮:默认 / 白底黑字 / 护眼绿
- 翻页衔接修复
- UTF-8 坏字节容错
- 行号隐藏
- 不可见字符弹窗关闭

## 1.0.0

- 首版:中文编码自动识别、只读、整页翻页、书签、章节、书库

> 1.1.0 及 0.x 的变更明细见 GitHub 提交历史。